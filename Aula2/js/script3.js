var nome_produton;
var preco;
var desconto;
var resultado;

nome_produto = prompt("Digite um produto", "");
preco = prompt("Digite um preço", "");
desconto = prompt("Digite o desconto", "");

preco = parseFloat(preco);
desconto = parseFloat(desconto);

preco = n2;
desconto = (preco * desconto) / 100;

console.log(nome);
console.log(resultado);
